<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="www.bugdr.ucenter.mapper.UcUserMapper">
    <select id="getUserByAccount" resultType="www.bugdr.ucenter.vo.UserVo">
        SELECT *
        FROM `uc_user` uu
                 LEFT JOIN `uc_user_info` uui ON uui.`user_id` = uu.`id`
        WHERE uu.`user_name` = #{name}
           OR uui.`email` = #{email};
    </select>
</mapper>
